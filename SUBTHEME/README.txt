Install Node.js https://nodejs.org/en/
Or check if you already have it...
$ node -v

Gulp
--
$ cd into SUBTHEME

$ sudo npm install gulp -g

$ npm init

$ npm install gulp gulp-compass gulp-autoprefixer gulp-clean-css gulp-uglify gulp-rename gulp-concat gulp-notify browser-sync gulp-plumber --save-dev
